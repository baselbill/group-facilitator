@AGENTS.md

# gstack

Use the `/browse` skill from gstack for all web browsing. Never use `mcp__claude-in-chrome__*` tools directly.

Available gstack skills:
- `/office-hours` — structured office hours session
- `/plan-ceo-review` — plan review for CEO
- `/plan-eng-review` — plan review for engineering
- `/plan-design-review` — plan review for design
- `/design-consultation` — design consultation session
- `/design-shotgun` — rapid design exploration
- `/design-html` — HTML design generation
- `/review` — code review
- `/ship` — ship a change
- `/land-and-deploy` — land and deploy a change
- `/canary` — canary deployment
- `/benchmark` — run benchmarks
- `/browse` — web browsing (use this instead of chrome MCP tools)
- `/connect-chrome` — connect Chrome browser
- `/qa` — QA session
- `/qa-only` — QA without shipping
- `/design-review` — design review session
- `/setup-browser-cookies` — set up browser cookies
- `/setup-deploy` — set up deployment
- `/setup-gbrain` — set up gbrain
- `/retro` — retrospective
- `/investigate` — investigate an issue
- `/document-release` — document a release
- `/document-generate` — generate documentation
- `/codex` — codex session
- `/cso` — CSO session
- `/autoplan` — automatic planning
- `/plan-devex-review` — plan review for developer experience
- `/devex-review` — developer experience review
- `/careful` — careful/cautious mode
- `/freeze` — freeze changes
- `/guard` — guard mode
- `/unfreeze` — unfreeze changes
- `/gstack-upgrade` — upgrade gstack
- `/learn` — learning session
